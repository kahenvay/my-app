import React from 'react';

const MoreDetailsWrapper = (props) =>{

    return(
        <div className={"moreDetailsWrapper"}>
            {props.children}
        </div>
    )
}

export default MoreDetailsWrapper;